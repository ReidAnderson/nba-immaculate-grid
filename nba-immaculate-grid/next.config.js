/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/nba-immaculate-grid',
    experimental: {
        appDir: true,
    }
}

module.exports = nextConfig
